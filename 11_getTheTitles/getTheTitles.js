const getTheTitles = function(book) {
let arr = [];

return arr = book.map(x => x.title)

};

// Do not edit below this line
module.exports = getTheTitles;

/*
obj (title, author / title, author)
>I need to filter the title and put that into a new array


*/